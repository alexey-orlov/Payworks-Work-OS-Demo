---
description: Run the PM→BA handoff readiness check.
argument-hint: "[initiative-slug] [job-id]"
---

Invoke the `pm-handoff` skill with the arguments below, unchanged.

$ARGUMENTS

> **Alias.** `/pm-ba-handoff` is Payworks' house name for the `pm-handoff` skill — same skill, one
> name. The skill folder keeps its OS name on purpose: renaming it would fork a
> master-maintained skill and stop future updates applying to this instance. House terms
> belong in document titles and prose; this file is the bridge. Mapping recorded in
> `os-installation/customization-status.md`.
