# Queries

Reusable SQL by area. Never commit customer-identifier cohorts inline.

**Read this when:** You need the SQL behind a metric or dashboard.

## Contents

### Subfolders

_None yet — one `{area}/` folder per product area in `feature-index.yaml`, created when that area's first query is filed. Add a one-line entry here and give the new folder a 5-line `CLAUDE.md` stub._
